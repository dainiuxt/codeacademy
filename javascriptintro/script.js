let person = {
    vardas: 'Dainius',
    pavarde: 'T.',
    age: 45
}

let asmuo = ['Dainius', 'T.', 45]

console.log(person.vardas + ' ' + person.pavarde + ' ' + person.age);
let newName = 'Petras';
person.vardas = newName;
console.log(person.vardas + ' ' + person.pavarde + ' ' + person.age);

// console.log(asmuo[0] + ' ' + asmuo[1] + ' ' + asmuo[2]);

// UZDUOTIS
let person = {
    name: // is prompt irasytas vardas,
    age: // is prompt irasytas vardas,
    city: // is prompt irasytas vardas,
}
// person per prompt gauta pvz.: vardas
// person per prompt gauta pvz.: age
// person per prompt gauta pvz.: city

// console.log